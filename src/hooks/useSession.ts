/**
 * useSession re-export.
 *
 * FIX: This file was 0 bytes. It exists as a convenience wrapper so
 * components can import from "@/hooks/useSession" rather than coupling
 * directly to "next-auth/react". Swap the implementation here if the
 * auth library ever changes — no component imports need updating.
 */
export { useSession } from "next-auth/react";